name: Test Simple Workflow

on:
  push:
    tags:
      - 'test-*'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Test
        run: |
          echo "Workflow is working!"
          echo "Branch: ${{ github.ref }}"
          echo "Tag: ${{ github.ref_name }}"
          ls -la desktop_alone/ | head -20

