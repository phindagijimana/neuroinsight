# NeuroInsight Desktop - Python Dependencies
# For PyInstaller bundling and desktop application

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database (SQLite for desktop mode - no PostgreSQL needed)
sqlalchemy==2.0.23
alembic==1.12.1

# Configuration
pydantic==2.5.0
pydantic-settings==2.1.0

# Neuroimaging
nibabel==5.1.0
numpy==1.26.2
pandas==2.1.3
matplotlib==3.8.2
pillow==10.1.0
nilearn==0.10.2

# Scientific Computing
scipy==1.11.4

# Deep Learning (PyTorch)
torch==2.1.1
torchvision==0.16.1

# Task Queue (for desktop mode - using threading, no Celery needed)
# celery==5.3.4  # Not needed for desktop threading mode
# redis==5.0.1   # Not needed for desktop mode

# Logging
structlog==23.2.0
python-json-logger==2.0.7

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
platformdirs==4.0.0
minio==7.2.0
psutil==5.9.6

# File handling
pathlib2==2.3.7.post1

# FastSurfer dependencies
# (FastSurfer is bundled, but needs these at runtime)
scikit-image==0.22.0
h5py==3.10.0

